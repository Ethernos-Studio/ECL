# 字符串转义序列

ECL 支持丰富的字符串转义序列，让您能够在字符串中包含特殊字符。

## 基本转义序列

### 换行符 (\n)
在字符串中插入换行：
```ecl
println("第一行\n第二行\n第三行");
// 输出：
// 第一行
// 第二行
// 第三行
```

### 制表符 (\t)
在字符串中插入水平制表符：
```ecl
println("Name\tAge\tCity");
println("Alice\t25\tNew York");
println("Bob\t30\tLondon");
// 输出对齐的表格数据
```

### 双引号 (\")
在字符串中包含双引号：
```ecl
println("She said \"Hello World!\"");
// 输出：She said "Hello World!"
```

### 单引号 (\')
在字符串中包含单引号：
```ecl
println("It\'s a beautiful day");
// 输出：It's a beautiful day
```

### 反斜杠 (\\)
在字符串中包含反斜杠：
```ecl
println("Windows路径: C:\\Users\\Documents");
println("Unix路径: /home/user/documents");
// 输出：Windows路径: C:\Users\Documents
// 输出：Unix路径: /home/user/documents
```

## Unicode转义序列

### \u{XXXX} 格式
支持1-6位十六进制的Unicode代码点：
```ecl
// 表情符号
println("表情: \u{1F600} \u{1F601} \u{1F602}");
// 输出：表情: 😀 😁 😂

// 中文字符
println("中文: \u{4E2D}\u{6587}\u{6D4B}\u{8BD5}");
// 输出：中文: 中文测试

// 数学符号
println("数学: \u{03C0} = 3.14159, \u{221A}2 = 1.414");
// 输出：数学: π = 3.14159, √2 = 1.414
```

### \uXXXX 格式
支持4位十六进制的Unicode代码点：
```ecl
// ASCII字符
println("ASCII: \u0041\u0042\u0043");
// 输出：ASCII: ABC

// 特殊符号
println("符号: \u2665 \u2605 \u00A9");
// 输出：符号: ♥ ★ ©
```

## 实际应用示例

### 1. 格式化表格输出
```ecl
func printStudentTable() {
    println("=== 学生成绩表 ===");
    println("姓名\t\t数学\t英语\t总分");
    println("------------------------");
    println("张三\t\t85\t92\t177");
    println("李四\t\t78\t88\t166");
    println("王五\t\t92\t85\t177");
}

printStudentTable();
```

### 2. 多行消息输出
```ecl
func showWelcomeMessage() {
    println("欢迎使用ECL编程语言！");
    println("");
    println("主要特性：");
    println("• 简洁的语法");
    println("• 强大的表达式函数");
    println("• 丰富的数据结构");
    println("• 友好的错误提示");
    println("");
    println("开始您的编程之旅吧！\u{1F680}");
}

showWelcomeMessage();
```

### 3. 文件路径处理
```ecl
func demonstrateFilePaths() {
    println("=== 文件路径示例 ===");
    
    // Windows路径
    var windowsPath = "C:\\Users\\Alice\\Documents\\project.ecl";
    println("Windows: " + windowsPath);
    
    // Unix路径
    var unixPath = "/home/alice/documents/project.ecl";
    println("Unix: " + unixPath);
    
    // 网络路径
    var networkPath = "\\\\server\\share\\folder\\file.txt";
    println("Network: " + networkPath);
}

demonstrateFilePaths();
```

### 4. Unicode艺术
```ecl
func printUnicodeArt() {
    println("=== Unicode艺术 ===");
    println("\u{2660} \u{2665} \u{2666} \u{2663} 扑克牌花色");
    println("\u{2600} \u{2601} \u{2602} \u{2603} 天气符号");
    println("\u{2654} \u{2655} \u{2656} \u{2657} \u{2658} \u{2659} 国际象棋");
    println("\u{1F300} \u{1F301} \u{1F302} \u{1F303} 表情符号");
}

printUnicodeArt();
```

## 错误处理

### 无效Unicode代码点
```ecl
// 无效的Unicode代码点会被当作字面量处理
println("无效: \u{ZZZZ} \u{12345678}");
// 输出：无效: \u{ZZZZ} \u{12345678}
```

### 不完整的转义序列
```ecl
// 不完整的转义序列也会被当作字面量处理
println("不完整: \u{123");
// 输出：不完整: \u{123
```

### 未知转义序列
```ecl
// 未知的转义序列会被当作字面量处理
println("未知: \\x \\y \\z");
// 输出：未知: \x \y \z
```

## 类型转换注意事项

在字符串连接中使用数组或列表元素时，需要注意类型转换：

```ecl
// 错误：不能直接连接字符串和数字
// var <int>scores[3] = {85, 92, 78};
// println("Score: " + scores[0]);  // 类型错误

// 正确：需要显式类型转换
var <int>scores[3] = {85, 92, 78};
println("Score: " + <str>scores[0]);  // 正确：输出"Score: 85"

// 列表元素也需要类型转换
var mixed = {1, "hello", true, 3.14};
println("Number: " + <str>mixed[0]);   // 需要转换
println("String: " + mixed[1]);        // 字符串不需要转换
println("Boolean: " + <str>mixed[2]);  // 需要转换
```

## 最佳实践

### 1. 使用转义序列提高可读性
```ecl
// 好的做法：使用转义序列
println("错误：文件未找到\n请检查路径：C:\\data\\file.txt");

// 不好的做法：多次print调用
print("错误：文件未找到");
println("");
print("请检查路径：C:\\data\\file.txt");
```

### 2. 合理使用Unicode字符
```ecl
// 增强用户体验
println("操作成功！\u{2705}");
println("操作失败！\u{274C}");
println("警告：\u{26A0} 请谨慎操作");
```

### 3. 注意转义序列的兼容性
```ecl
// 在需要兼容性时使用基本转义
println("基本转义：\n\t\"Hello\\World\"");

// 在支持Unicode的环境中使用Unicode转义
println("Unicode转义：\u{1F600} \u{1F44D} \u{2764}");
```

## 性能考虑

转义序列的处理在词法分析阶段完成，对运行时性能影响极小。可以放心使用转义序列来增强字符串的表现力。

## 总结

ECL的字符串转义序列提供了：
- **基本转义**：`\n`, `\t`, `\"`, `\'`, `\\`
- **Unicode支持**：`\u{XXXX}` 和 `\uXXXX` 格式
- **错误处理**：无效转义序列会被安全处理
- **灵活性**：支持各种文本格式化需求

通过合理使用转义序列，您可以创建更加生动和用户友好的输出内容。